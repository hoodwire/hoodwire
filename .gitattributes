# Normalize line endings to LF in the repository; check out native on each OS.
* text=auto eol=lf

# Binary / generated files: never diff or normalize.
package-lock.json linguist-generated=true
*.png binary
*.jpg binary
*.woff2 binary
