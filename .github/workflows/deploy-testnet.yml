name: Deploy to Robinhood testnet

# Manual only — never runs automatically.
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    name: Deploy contracts to Robinhood Chain Testnet
    runs-on: ubuntu-latest
    env:
      RPC_URL: https://rpc.testnet.chain.robinhood.com
      CHAIN_ID: "46630"
      EXPLORER: https://explorer.testnet.chain.robinhood.com
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - uses: foundry-rs/foundry-toolchain@v1

      - name: Install forge-std
        working-directory: contracts
        run: |
          git config --global user.email "ci@hoodwire.local"
          git config --global user.name "hoodwire ci"
          forge install foundry-rs/forge-std

      - name: Run contract tests
        working-directory: contracts
        run: forge test -vvv

      - name: Check deployer key and gas
        working-directory: contracts
        env:
          DEPLOYER_KEY: ${{ secrets.TESTNET_DEPLOYER_KEY }}
        run: |
          if [ -z "$DEPLOYER_KEY" ]; then
            echo "::error::Secret TESTNET_DEPLOYER_KEY is empty or not set. Add it under Settings -> Secrets and variables -> Actions (click the green 'Add secret' button), then re-run."
            exit 1
          fi
          case "$DEPLOYER_KEY" in 0x*) ;; *) DEPLOYER_KEY="0x$DEPLOYER_KEY" ;; esac
          ADDR=$(cast wallet address --private-key "$DEPLOYER_KEY")
          echo "deployer: $ADDR"
          BAL=$(cast balance "$ADDR" --rpc-url "$RPC_URL")
          echo "balance (wei): $BAL"
          if [ "$BAL" = "0" ]; then
            echo "::error::deployer $ADDR has 0 ETH — fund it at https://faucet.testnet.chain.robinhood.com"
            exit 1
          fi

      - name: Deploy
        working-directory: contracts
        env:
          DEPLOYER_KEY: ${{ secrets.TESTNET_DEPLOYER_KEY }}
        run: |
          case "$DEPLOYER_KEY" in 0x*) ;; *) DEPLOYER_KEY="0x$DEPLOYER_KEY" ;; esac
          forge script script/Deploy.s.sol:Deploy \
            --rpc-url "$RPC_URL" \
            --broadcast \
            --private-key "$DEPLOYER_KEY" \
            --slow -vvv

      - name: Extract public addresses
        run: node scripts/extract-deployment.mjs "$CHAIN_ID" robinhood-testnet "$RPC_URL" "$EXPLORER"

      - name: Commit deployment addresses
        run: |
          git config user.email "ci@hoodwire.local"
          git config user.name "hoodwire ci"
          git add deployments/robinhood-testnet.json
          git commit -m "chore(deploy): record Robinhood testnet contract addresses" || echo "no changes"
          git push
